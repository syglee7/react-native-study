import React from 'react';
import {Button, SafeAreaView, StyleSheet, View} from 'react-native';

function AuthHomeScreen() {
  return (
    <SafeAreaView>
      <View>
        <Button title="로그인 화면으로 이동" />
      </View>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({});

export default AuthHomeScreen;
